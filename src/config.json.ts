const config = {
  slotSize: 96,
  slotNumber: [3, 3],
  marginSize: 10,
  outerMarginSize: 20,
  container: '.scratchcard',
  options: {
    transparent: true,
    antialias: true,
    resolution: 1
  }
}

export default config;